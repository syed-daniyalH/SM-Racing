# Local development can proxy through the Next.js rewrite.
NEXT_PUBLIC_API_URL=/api/v1

# In Vercel production, point this at the Render backend, for example:
# NEXT_PUBLIC_API_URL=https://sm-racing-api.onrender.com/api/v1
