# Local development can proxy through the Next.js rewrite.
NEXT_PUBLIC_API_URL=/api/v1

# For Vercel or any production deployment, point this at your hosted backend,
# for example:
# NEXT_PUBLIC_API_URL=https://api.example.com/api/v1
